+++
title = "Keyboard shortcuts"
description = "Global navigation, completion, and list or graph interaction shortcuts in the Claimframe desktop application."
layout = "docs"
[[related]]
label = "Query syntax"
url = "/guide/reference/query-syntax/"
+++

## Global shortcuts

| Action | macOS | Windows and Linux |
| --- | --- | --- |
| Open a fresh capture | `Cmd` + `Shift` + `C` | `Ctrl` + `Shift` + `C` |
| Open quick assertion query | `Ctrl` + `Shift` + `Q` | `Ctrl` + `Shift` + `Q` |
| Open the command palette | `Cmd` + `K` | `Ctrl` + `K` |

The quick-query shortcut currently uses `Ctrl` on every platform.

## Capture and query completion

| Action | Shortcut |
| --- | --- |
| Explicitly show suggestions | `Ctrl/Cmd` + `Space` |
| Move through visible suggestions | `Up Arrow` or `Down Arrow` |
| Accept the selected suggestion | `Tab` |
| Run a query | `Enter` |

Autocomplete may also appear automatically after at least two characters in the active token.

## General interaction

Standard keyboard behavior applies to buttons, links, menus, and form controls. Available graph-specific commands are exposed through focusable graph controls rather than a separate global shortcut set.
